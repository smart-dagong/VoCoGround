# VoCoGround
VoCoGround is a multimodal system that enables users to locate objects in an image using natural spoken language. By combining camera input and voice instructions, the system performs referring expression reasoning and returns precise bounding boxes for the target object.
